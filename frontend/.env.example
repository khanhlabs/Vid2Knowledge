# Public browser configuration. The anon key is safe to expose only with correct
# Supabase RLS/Auth policies; never place the service-role key in VITE_* variables.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-supabase-anon-key
